# Base URL of the LPU Umbrella backend API, including the /api/v1 prefix.
# The backend's CORS_ORIGIN must match the origin this app runs on
# (defaults to http://localhost:3000 - see vite.config.ts).
VITE_API_BASE_URL=https://lpubrella-production.up.railway.app/api/v1
